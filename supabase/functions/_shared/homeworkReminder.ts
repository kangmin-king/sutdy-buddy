// 숙제 미시작 알림의 기본 시각 — **이 파일이 유일한 정의다.**
//
// 브라우저 앱(src/constants.ts가 그대로 re-export)과 Edge Function이 같은 값을 읽고,
// DB 컬럼에는 기본값을 두지 않는다(0023 마이그레이션). 예전에는 같은 "21:00"이 세 군데
// (컬럼 기본값 · 앱 상수 · 함수 상수)에 박혀 있어서 한 곳만 고치면 조용히 어긋났다.
//
// 왜 여기냐: `_shared/`는 두 런타임이 모두 확실히 집어가는 유일한 폴더다. Edge Function은
// 이미 이 폴더를 상대 경로로 import하고(배포 번들에 들어간다), Vite·tsc는 프로젝트 안의
// 어느 파일이든 import를 따라간다. 반대 방향(src/에 두고 함수가 가져가기)은 함수 배포
// 번들러가 supabase/ 밖을 어떻게 다루는지에 기대게 되어 피했다.
//
// ⚠️ 이 파일은 브라우저 번들에도 들어간다 — Deno API(Deno.env 등)를 여기 쓰면 앱 빌드가 깨진다.
// 순수 상수만 둘 것.

/** "HH:MM" (Asia/Seoul). 설정 행이 없는 학생에게 적용된다. */
export const DEFAULT_HOMEWORK_REMIND_AT = '21:00';
